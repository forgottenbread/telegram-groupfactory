apiVersion: v1
kind: Service
metadata:
  name: groupfactory
  namespace: groupfactory
  labels:
    app.kubernetes.io/name: groupfactory
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: groupfactory
    app.kubernetes.io/component: app
  ports:
  - name: http
    port: 8000
    targetPort: http
